services:
  discord-auth-bot:
    build: .
    container_name: discord-auth-bot
    restart: always
    ports:
      - "3002:3002"
    env_file:
      - .env
    volumes:
      - ./db:/app/db
      - ./data:/app/data